<h1>タスク一覧</h1>

<ul>
  
<% @tasks.each do |task| %>

    <li>
<%= link_to task.id, task %>
 : 
<%= task.content %>
&gt;&gt;
<%= task.status %> 
<%= link_to '消去', task, method: :delete, data: { confirm: 'タスクを消してよろしいですか？' } %>
</li>
  
<% end %>

</ul>


<%= link_to '新規タスクの投稿', new_task_path %>